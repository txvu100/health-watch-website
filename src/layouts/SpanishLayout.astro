---
import BaseLayout from './BaseLayout.astro'

interface Props {
  title: string
  description: string
  canonical?: string
  ogImage?: string
  schemas?: Record<string, unknown>[]
  enPath?: string
  noindex?: boolean
}

const props = Astro.props
---

<BaseLayout lang="es" {...props}>
  <slot />
</BaseLayout>
